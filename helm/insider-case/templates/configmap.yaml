apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "insider-case.fullname" . }}-config
  labels:
    {{- include "insider-case.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}